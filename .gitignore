node_modules/
.next/
.env.local
.env*.local
*.log
.DS_Store

# Espelho interno do manual do LANC (procedimentos de biossegurança,
# sacrifício animal, códigos de descarte FURB) — não é dado de pesquisa,
# mas também não é conteúdo público do site. Mantido só localmente até
# decisão sobre visibilidade do repositório.
content/referencias-manual/*.txt
